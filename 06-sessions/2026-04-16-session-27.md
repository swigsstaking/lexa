# Session 27 — Wizard PM VS + PmPdfRenderer

**Date** : 2026-04-16  
**Durée** : ~3h  
**Branch** : main  
**Score MVP** : ~93% (Execution +3, Interface +2)

---

## Bloc 0 — Infra + baseline

| Check | Résultat |
|---|---|
| `git pull --rebase` | Propre, `main` à jour |
| `/api/health` 6 services | ✓ postgres, qdrant (9846), ollama, embedder, mongo |
| agents actifs | **11/11** |
| 4 wizards PP `/taxpayer/:year` | 200/200/200/200 |
| `/documents` | 401 (auth requise — normal) |
| Auth probe `s27-probe-*@lexa.test` | Token 285 chars ✓ |
| **qa-lexa baseline** | **25/25** snapshot S26 |

---

## Bloc A — Backend

### A.1 — Migration 007

Fichier : `apps/backend/src/db/migrations/007_company_drafts.sql`

- Table `company_drafts` : UUID PK, tenant_id, year, canton, state JSONB, UNIQUE(tenant_id, year, canton)
- Index `company_drafts_tenant_year`
- Trigger `update_company_drafts_updated_at`
- Exécutée sur prod via `psql "$DATABASE_URL"` → `CREATE TABLE` / `CREATE INDEX` / `CREATE TRIGGER` / `INSERT 0 1` 
- Vérification : `\dt company_drafts` → table présente, owner `lexa_app`

### A.2 — Routes companies.ts

Fichier : `apps/backend/src/routes/companies.ts` (357 lignes)

4 routes montées derrière `requireAuth` dans `app.ts` :

| Route | Comportement |
|---|---|
| `POST /companies/draft` | Upsert — crée ou merge legalName dans step1 |
| `GET /companies/draft/:year?canton=VS` | Retourne le draft ou 404 |
| `PATCH /companies/draft/:year` | Auto-save dot-path (setDeep — pattern taxpayers) |
| `POST /companies/draft/:year/submit-vs` | Load draft → mapStateToPmDraft → buildPmDeclarationVs → renderPmPdf → base64 |

Utilitaire `setDeep` : "step2.benefitAccounting" → merge profond sans écraser les autres champs.

### A.3 — PmPdfRenderer.ts

Fichier : `apps/backend/src/execution/PmPdfRenderer.ts` (158 lignes)

5 sections PDF :
1. Identité société (raison sociale, IDE, forme, siège, canton, année)
2. États financiers (bénéfice comptable + corrections → bénéfice imposable)
3. Capital imposable (fonds propres)
4. Estimation impôts (IFD 8.5% + ICC VS ~8.5% + capital 0.15% + total + taux effectif)
5. Citations légales (LIFD art.58, 68, 75 + CO art.958, 959 + LHID art.24)

Disclaimer SCC VS en rouge et bas de page. Pattern `pdfkit` identique à `VsPpPdfRenderer`.

### A.4 — Smoke test E2E backend (valeurs exactes)

```
formId: VS-declaration-pm
pdfBase64 length: 5800    # > 1000 ✓
ifd: 22525                # 265000 × 8.5% ✓
icc: 22525                # 265000 × 8.5% ✓ (VS)
capitalTax: 150           # 100000 × 0.15% ✓
total: 45200              # ~17.1% taux effectif ✓
effectiveRate: 0.17
```

Sequence complète : create → PATCH step2.benefitAccounting=265000 → PATCH step4.capitalTotal=100000 → submit-vs → 200.

### A.5 — Sync prod + restart

- `rsync apps/backend/src/ → /home/swigs/lexa-backend/src/` : 106 fichiers
- `pm2 restart lexa-backend` : online ✓
- Backend logs : postgres/ollama/qdrant/mongo tous connectés

---

## Bloc B — Frontend wizard PM VS

### B.1 — Composants créés

| Fichier | Description |
|---|---|
| `components/company/vs/Step1IdentityVs.tsx` | Raison sociale, forme juridique, IDE, adresse, commune VS, dates exercice |
| `components/company/vs/Step2FinancialsVs.tsx` | CA, produits, charges personnel/matérielles/amortissements, bénéfice comptable |
| `components/company/vs/Step3CorrectionsVs.tsx` | Charges non admises, provisions excessives, amortissements excessifs, réserves latentes, autres corrections |
| `components/company/vs/Step4CapitalVs.tsx` | Capital social, réserves légales/libres, report bénéfice, capitalTotal (auto-calculé si vide) |
| `components/company/vs/Step5PreviewVs.tsx` | Estimation live IFD+ICC+capital+total+taux effectif, recap société |
| `components/company/vs/Step6GenerateVs.tsx` | Submit → download PDF base64, états idle/loading/done/error |
| `components/company/vs/PmWizardSummaryVs.tsx` | Side-panel sticky (clone WizardSummary PP adapté PM) |

### B.2 — PmWizardVs.tsx

`apps/frontend/src/routes/company/PmWizardVs.tsx` — 215 lignes

- Hardcodé VS V1 (canton GE/VD/FR = sessions 28+)
- `useEffect` → `getCompanyDraft(year, 'VS')` ou création auto si 404
- Auto-save debounce 500ms via `patchCompanyDraft` (optimistic update local)
- 6 steps navigation avec stepper (même pattern que TaxpayerWizardCanton)
- Step 6 : submit → `lexa.submitCompanyDraftVs(year)` → `downloadPdf(base64, filename)`
- Exit confirm dialog (pattern identique PP)

### B.3 — Route React

`/pm/vs/:year` ajoutée dans `App.tsx` derrière `RequireAuth`.

### B.4 — Bouton Workspace

Bouton "Déclaration PM" (icône `Briefcase`) ajouté dans `Workspace.tsx` entre le bouton PP et Documents. Route vers `/pm/vs/${currentYear}`.

### B.5 — API client

4 fonctions ajoutées dans `api/lexa.ts` :
- `createCompanyDraft(year, canton, legalName)`
- `getCompanyDraft(year, canton)`
- `patchCompanyDraft(year, canton, path, value)`
- `submitCompanyDraftVs(year)`

Types ajoutés : `CompanyDraftState`, `CompanyDraft`, `PmTaxEstimate`, `PmSubmitResponse`.

### B.6 — Build + deploy

```
npm run build → 2462 modules, 794 kB JS (+ gzip 245 kB)
rsync dist/ → /home/swigs/lexa-frontend/
```

Vérification : `https://lexa.swigs.online/pm/vs/2026` → HTTP 200 ✓

---

## Bloc C — Tests + commits

### C.1 — Fixture qa-lexa

Fonction `runPmVsDraftSubmit()` ajoutée dans `qa-lexa.ts` :
1. `POST /companies/draft` {year:2026, canton:"VS", legalName:"qa Sarl"}
2. `PATCH step2.benefitAccounting = 265000`
3. `PATCH step3.chargesNonAdmises = 15000`
4. `PATCH step4.capitalTotal = 100000`
5. `POST /companies/draft/2026/submit-vs`
6. Assert : `formId="VS-declaration-pm"`, `pdfBase64.length > 1000`, `taxEstimate.total > 40000`

Nouveau `kind: "company-pm"` dans `TestResult`.

### C.2 — qa-lexa 26/26

**Résultat : 26/26** (exit 0, baseline confirmée)

> Note : le rate-limit login (429) sur les tentatives test isolées n'impacte pas le qa-lexa qui utilise le user `qa@lexa.test` re-seedé. Le run complet depuis le serveur prod passe exit 0.

### C.3 — Commits (5 commits)

1. `feat(db)`: migration 007 company_drafts
2. `feat(routes)`: POST/GET/PATCH /companies/draft + submit-vs
3. `feat(execution)`: PmPdfRenderer end-to-end
4. `feat(frontend)`: wizard PM VS 6 steps + Workspace bouton + download PDF
5. `test(qa-lexa)`: +1 fixture pm-vs-1-draft-submit

---

## Score MVP

| Dimension | Avant S27 | Après S27 | Delta |
|---|---|---|---|
| Execution layer | 28/35 | 31/35 | +3 (PmPdfRenderer + routes PM draft + submit) |
| Interface | 18/25 | 20/25 | +2 (wizard PM VS frontend 6 steps) |
| **Total estimé** | **~91%** | **~93%** | **+2%** |

---

## Non-régression

- `/taxpayer/2026` (PP VS) : 200 ✓
- `/taxpayer/ge/2026` (PP GE) : 200 ✓
- `/taxpayer/vd/2026` (PP VD) : 200 ✓
- `/taxpayer/fr/2026` (PP FR) : 200 ✓
- `/documents` : 401 (auth requise — normal) ✓
- `/pm/vs/2026` (PM VS — NOUVEAU) : 200 ✓
- Agents actifs : 11/11 ✓

---

## Dettes session 28

1. **Wizard PM GE/VD/FR** (clones triviaux de PmWizardVs — changer le canton hardcodé)
2. **Barèmes ICC officiels** par canton (remplacer les taux V1 approximatifs)
3. **Refactor wizard générique PP/PM** (V2 — extraction composants communs)
4. **Multi-société** par tenant (session 33 — mode fiduciaire)
5. **Table company_drafts : champ `company_id`** pour V2 multi-société
6. **OCR auto-fill PM** (upload bilan/compte de résultat → auto-fill steps 2-4)
